# Decisions Log

Short record of product/design decisions made for this app. Check this file before
starting new work — if a new task conflicts with something here, flag it instead of
silently overriding it.

## Theme

- Default theme = system preference (`prefers-color-scheme`), fallback to **light** if unavailable. Never hardcode a default. (2026-09-02)
- Theme toggle lives as a standalone icon button in the Header's quick-action icons, placed **before** the Settings button — not inside the profile dropdown. (2026-09-02)
- Toggle icon: `lucide-react`'s `Moon` (dark theme active) / `Sun` (light theme active), 22px. Chosen over Material Symbols `dark_mode`/`light_mode` and other lucide pairs (`MoonStar`/`SunMedium`, `CloudMoon`/`CloudSun`) after visual comparison — first icon in the header to use lucide-react instead of Material Symbols. (2026-09-02)
- Highlight/brand accent recolored from indigo/violet (`#4648d4`/`#6063ee`/`#6366f1`/`#8b5cf6`/`#c0c1ff`) to emerald green, across `tailwind.config.js`'s `primary` token, `theme.css`'s `--primary*` variables (both unused currently — no component reads them yet), and the hardcoded arbitrary-value colors in `Header.tsx`/`Sidebar.tsx`. Light: `#059669`/`#10b981`. Dark: `#10b981`/`#34d399`. Light accent-on-dark tint: `#6ee7b7`. Chosen over matching the existing `secondary` green or a darker forest green, after offering both as options. (2026-09-04)
- Active sidebar nav item switched from a solid gradient-fill pill (white text) to a flat pastel pill matching a user-provided reference: light mode `bg-[#d1fae5]` / `text-[#065f46]`, dark mode `bg-[#052e1c]` / `text-[#6ee7b7]`, icon colored to match the text. No shadow, no gradient. (2026-09-04)
- `accentColors.ts`'s Violet accent base color corrected to `#6366f1` (indigo-500, the same hue removed from the app-wide palette back on 2026-09-04) — previously `#8b5cf6` (violet-500). Dark mode uses `#6366f1` directly as `solid`; light mode uses one shade darker (`#4f46e5`) for contrast against white, matching the existing green accent's dark/light shade convention. All dependent tokens (hover, gradient, pastel, tint) rescaled to the indigo Tailwind scale to match. (2026-09-05)

## Chat

- Added a floating AI chat button (`ChatButton.tsx`): fixed to the right edge (`right-0`), icon-only (no label), `BotMessageSquare` (lucide-react, user-picked over `MessageCircle`/`Sparkles`/`Bot`/`MessageSquare`). Styled to match the Sidebar logo badge's brand gradient (dark: `from-[#10b981] to-[#34d399]`, light: `from-[#059669] to-[#10b981]`). `onClick` currently just logs — placeholder until wired to the AI backend. (2026-09-04)
- Chat button reshaped from a floating circle to a half-pill tab flush against the screen's right edge: `rounded-l-full` / `rounded-r-none`, small asymmetric padding (`pl-2.5 pr-3`, widens slightly on hover) so it reads as merged into the edge rather than floating. Icon size kept at 26px — only the surrounding padding was shrunk. (2026-09-04)
- Added `ChatPopup.tsx`, opened by clicking the `ChatButton`: a fixed `bottom-24 right-4` card (`380px` wide, `520px` tall) with a header ("AI Assistant" + close), a scrollable message list, and a text input + send button. User bubbles are solid accent-filled (right-aligned); assistant bubbles are flat neutral pastel (left-aligned) — no gradients, per the existing flat-UI convention. Icons (user-picked over alternatives offered): close `X`, send `SendHorizontal`, copy `Copy` (swaps to `Check` + "Copied" label for 1.5s after click). Sending a message appends a placeholder assistant reply (no AI backend wired yet, same placeholder pattern used elsewhere). The `ChatButton` is hidden while the popup is open (popup has its own close button) to avoid it peeking out from behind the popup's right edge. State (`messages`, `input`) is local/self-contained in `ChatPopup`, not lifted to `App.tsx`. (2026-09-05)
- "Copy" button appears only under the most recently received assistant message (not every message), per the literal ask to copy "the last response". (2026-09-05)
- `ChatPopup` is now vertically draggable: its header (the "AI Assistant" title bar) acts as a drag handle (`cursor-grab`/`cursor-grabbing`, mouse + touch), letting the user slide the whole popup up/down the screen. Position is tracked as a `bottom` pixel offset in local state (not lifted to `App.tsx`), clamped so at least `160px` of the popup stays on-screen when dragged toward the top and it can't go below `8px` from the bottom edge. The popup's `max-height` is derived from that same offset (`calc(100vh - bottom - 16px)`) so it shrinks instead of overflowing off the top of the viewport as it's dragged higher. Per explicit user request, it always reopens at the original default bottom position (`96px`, matching the prior fixed `bottom-24`) regardless of where it was last dragged to — the offset resets via a `useEffect` keyed on `isOpen`, it isn't persisted across opens (unlike `ChatButton`'s drag position, which does persist via `localStorage`). Horizontal position (`right-4`) is unchanged/not draggable. (2026-09-05)

## Icons

- Replaced **all** Material Symbols Outlined icons app-wide with `lucide-react`, for one consistent icon system (started as just the theme toggle, extended to the whole app). Google Fonts Material Symbols link removed from `index.html`; `.material-symbols-outlined` CSS rule removed from `theme.css`. (2026-09-02)
- Mapping used — Header: `search`→`Search`, `expand_more`→`ChevronDown`, `check`→`Check`, `add_circle`→`CirclePlus`, `settings`→`Settings`, `account_circle` (Support button)→`LifeBuoy`. Sidebar: `dashboard`→`LayoutDashboard`, `lock`→`Lock`, `upload_file`→`FileUp`, `account_balance_wallet`→`Wallet`, `settings`→`Settings`, `help`→`LifeBuoy`. `LifeBuoy` used for Support in both Header and Sidebar for consistency (previously mismatched: `account_circle` in Header vs `help` in Sidebar for the same concept). (2026-09-02)

- Header's Settings icon button removed (Settings already lives in the Sidebar footer — was a duplicate). Header's Support (`LifeBuoy`) button replaced with an Alerts button (`Bell`, lucide-react, chosen over `BellRing`/`AlertTriangle`). Shows a small red (`#ef4444`) dot badge, top-right of the icon, only when `hasAlerts` is true (prop on `Header`, no count yet — just unread/no-unread). (2026-09-02)
- Clicking the Bell now opens a Notifications dropdown (same positioning/shadow/border pattern as the Profile dropdown), self-contained in `Header.tsx` with local mock `notifications` state (title/description/time/read), same "ship UI-first with placeholder data" pattern as `ChatPopup`/Income mock data — no backend yet. Fixed-size (`w-[480px]` — widened 50% from an initial `w-80` per follow-up request, list `max-h-80 overflow-y-auto`) so a long notification list scrolls inside the dropdown instead of growing it. Header row has a "Mark all read" action (`CheckCheck` icon, user-picked over `Check`/`MailOpen`); each row is a red-dot unread indicator (no per-notification-type icons, kept deliberately minimal per the existing "no count yet — just unread/no-unread" precedent above). The bell's badge dot now reflects `hasAlerts` OR any unread mock notification. (2026-09-05)
- Per explicit follow-up request, notifications now **auto-remove the instant they're read** rather than staying visible with a cleared unread dot — clicking a row (or "Mark all read") removes it from state immediately (`dismissNotification`/`setNotifications([])`), so the `read` flag was dropped from the `Notification` type entirely (every notification still in the list is by definition unread). Mock seed data trimmed from 6 items (3 pre-marked read) to just the 3 unread ones, since a pre-read mock item would never be visible. (2026-09-05)
- Both the Profile and Alerts dropdowns' close-on-outside-click was switched from a per-dropdown `fixed inset-0` overlay `<div>` to a shared `document`-level `mousedown` listener (`useEffect` + `profileRef`/`alertsRef`), per explicit follow-up request. Root cause of the old approach not working outside the header: `Header`'s `backdrop-blur-md` makes it a containing block for `position: fixed` descendants (same `backdrop-filter` gotcha already documented above for `ProfileModal`), so the `fixed inset-0` overlay was actually confined to the header's own 64px-tall box instead of the full viewport — clicks anywhere in the main content area never reached it. (2026-09-05)

## Sidebar

- Removed the "Encrypted Sync / TLS 1.3" status box (was placeholder/inaccurate — no actual sync or TLS claim was backed by real functionality). Replaced with one plain line: "100% Private & Offline" with a small red (`#ef4444`) filled `Heart` (lucide-react) pinned to the right edge (`justify-between`), muted text color, no card/border, single line (`whitespace-nowrap`). (2026-09-02)
- Brand block (logo) height set to `h-16` (64px) to match the Header's row height exactly, flush to the sidebar's top edge — reads as one continuous top bar with the Header instead of floating lower with its own margin. (2026-09-02)
- Removed the internal divider lines (`border-b` under the logo, `border-t` above the Settings/Support footer) — sidebar sections now flow without separating lines. (2026-09-02)
- Sidebar is collapsible: toggle button (`PanelLeftClose`/`PanelLeftOpen`, lucide-react) next to the logo collapses it from `w-64` to an icon-only `w-20` rail (labels hidden, `title` tooltips added). Collapse state lives in `App.tsx` and is passed down to both `Sidebar` and `Header` so the Header's `left` offset and the main content's `margin-left` animate in sync (`transition-all duration-200`). (2026-09-02)
- Nav items updated to: Dashboard, Account, Income, Investment, Expense, Mapper, Vault, Taxes (`Ingestion` removed). Icons (all `lucide-react`, chosen to avoid duplicates with existing icons): Account→`User`, Income→`Banknote`, Investment→`TrendingUp`, Expense→`Receipt`, Taxes→`Landmark`. Dashboard/Mapper/Vault kept their existing icons (`LayoutDashboard`/`Wallet`/`Lock`). (2026-09-04)

## Header: Financial Entity Switcher

- Added a small edit icon (`Pencil`, lucide-react, 14px, chosen over `PencilLine`/`Settings2`) to each row in the "Switch Financial Entity" dropdown, right-aligned. New `onEditProfile?: (profile: Profile) => void` callback prop on `Header` — no-op default, no edit modal/UI built yet (placeholder, same pattern as `onOpenAddTransaction`). Row markup changed from `<button>` to a `role="button"` `<div>` so the edit button can nest inside without invalid HTML; edit click calls `stopPropagation()` so it doesn't also trigger entity selection. (2026-09-04)
- Removed the `Check` checkmark that previously marked the currently selected entity in the dropdown — rows no longer show a selected-state indicator beyond the existing highlighted background/bold text. (2026-09-04)
- Added a working "Add Financial Entity" row (`UserPlus` icon, user-picked over `CirclePlus`/`Plus`) below the entity list, and wired the previously-placeholder pencil edit button to an actual editor — both open a new `ProfileModal.tsx` (name input + a live avatar preview + a "Logo Color" swatch picker; a solid-color circle with an initial, not an uploaded image, per user's explicit choice of "pick an avatar color + initial" over an image-upload option). New independent `src/constants/avatarColors.ts` (`AVATAR_COLOR_OPTIONS`, 8 fixed hex swatches: red/orange/amber/green/cyan/blue/indigo/pink) — deliberately separate from `accentColors.ts`'s 3-color app-wide Theme Color accent, same "separate palette" precedent as `IncomeOverview.tsx`'s stat-card `ACCENTS`. `Profile` gained an optional `color?: string`; when unset, avatars keep falling back to the app's accent color/neutral styling as before (default seed profiles are unaffected). `App.tsx`'s `profiles` const was promoted to `useState` so adds/edits actually persist; `onEditProfile` now fires on Save with the fully-updated `Profile` (previously just logged the clicked row on pencil click) and a new `onAddProfile` prop fires on creating one — `ProfileModal` generates the new id itself (`crypto.randomUUID()`, falling back to a timestamp). (2026-09-05)
- `ProfileModal` renders via `createPortal(..., document.body)` instead of returning its JSX inline. It's opened from inside `Header.tsx`'s `<header>`, which has `backdrop-blur-md` — `backdrop-filter` creates a new containing block for `position: fixed` descendants (same effect as `filter`/`transform`), so the modal's `fixed inset-0` was being confined to the header's own box instead of centering on the viewport. Portaling to `document.body` is the standard React fix and matches how `SettingsModal`/the Income modals avoid the issue already (they're rendered from `App.tsx`/`IncomePage.tsx`, outside any blurred/filtered ancestor). (2026-09-05)
- All three user-facing "Financial Entity" strings ("Switch Financial Entity" dropdown label, "Add Financial Entity" row, `ProfileModal`'s "Add/Edit Financial Entity" title) renamed to "Profile" ("Switch Profile" / "Add Profile" / "Add/Edit Profile"), per explicit user request — matches the `Profile` type name already used internally and the "profile switcher"/"currentProfile" naming used throughout `Header.tsx`/`App.tsx`. Underlying prop/handler names (`onEditProfile`, `onAddProfile`, `onSelectProfile`) were already "Profile"-named and are unchanged. (2026-09-05)

## Income Page

- Built `IncomePage.tsx`, rendered by `App.tsx` when the sidebar's Income tab is active. Heading: "Income Stream & Payslip".
- Sub-navbar under the heading: a "Dashboard" pill tab, one pill tab per income stream, then a `SquarePlus` (lucide-react, user-picked over `Plus`/`CirclePlus`) icon button to add a new stream. Tabs reuse the existing flat pastel-pill active style (light `bg-[#d1fae5]`/`text-[#065f46]`, dark `bg-[#052e1c]`/`text-[#6ee7b7]`).
- The `SquarePlus` button opens a modal to create a stream: name text input + a flat pill segmented Taxable/Non-Taxable toggle (same pastel-pill style, defaults to Taxable). Submitting adds the stream, switches to its tab, and closes the modal.
- A solid "Add Payslip" button (`FilePlus`, lucide-react, user-picked over `FileText`/`ClipboardList`) sits at the far right of the sub-navbar — shown only when an income-stream tab is active, hidden on the "Dashboard" tab. Currently a placeholder (`onAddPayslip` prop, no-op console.log default, same pattern as other unwired handlers in `App.tsx`). (2026-09-04)
- Visual polish pass (no copy/content changes): tab pills now sit inside one grouped segmented-control container (bordered, subtle bg) instead of floating loose, matching the Tax Status toggle's existing grouped-pill look. Content area wrapped in a bordered card. Modal field labels switched to the small uppercase/bold/tracking-wider style already used for the Header dropdown's "Switch Financial Entity" section label, for one consistent label style app-wide. Rounded corners softened to `rounded-xl`/`rounded-2xl` on cards, buttons, and the modal; spacing increased throughout (heading, navbar, modal fields) for more breathing room. (2026-09-04)
- The "Dashboard" sub-tab (only) was rebuilt as a full bento-style overview (`IncomeOverview.tsx`), based on a user-provided mockup screenshot, recolored from the mockup's indigo/purple tones to this app's established green/pastel palette (green/blue/amber/pink accents per stat card, no gradients, per the earlier flat-UI decision above) rather than copied verbatim. Individual income-stream tabs keep the prior simple "no payslips yet" card, unchanged. Structure: a tinted header card (badge + eyebrow + title + description + "Ingest Payslip (PDF)" and "Add Income Stream" buttons, the latter opens the existing add-stream modal), 4 stat cards (Total Monthly Inflow/Primary Employment/Dividends & Capital Yield/Consulting & Advisory) each with an icon badge and a "Updated Xh/d ago" line, a 12-month stacked-bar + trend-line chart, and a donut "Income Diversity Index" chart with a center label and legend. All numbers/chart data are static placeholder mock data (no backend yet) — swap for real data once income/payslip data model exists. Icons (lucide-react, user-picked): `PiggyBank` (inflow), `Briefcase` (both Primary Employment and Consulting & Advisory — user picked `Briefcase` for both despite the duplicate-icon tradeoff being flagged), `LineChart` (dividends). Added `recharts` (^3.10.1) as a new dependency for the two charts — first charting library in the project. (2026-09-04)
- The Add Income Stream modal's Taxable/Non-Taxable toggle was changed from a fully-rounded pill (`rounded-full`) to a square-cornered segmented control (`rounded-lg` container, `rounded-md` buttons), per the user explicitly wanting a squarer look after seeing the boxy mockup above. (2026-09-04)
- Main content area background (`App.tsx`'s `<main>`, app-wide, not just Income) shifted from pure `bg-white`/`bg-[#0e0e0e]` to a subtly tinted `bg-[#f5f5fa]` (light) / `bg-[#0f0f14]` (dark), matching the faint canvas-vs-card contrast in the same mockup — Sidebar and Header keep their original pure white/`#0e0e0e`/`#131313` so the content canvas now reads as a distinct layer behind the (still pure white/dark) cards. (2026-09-04)
- `IncomeOverview.tsx` header card restructured: the "Ingest Payslip (PDF)" / "Add Income Stream" buttons were removed from the header card itself and moved to their own always-visible row directly below it (previously they'd have been hidden behind a view where zero income streams exist, since the only other place a stream-adding control lives — `IncomePage.tsx`'s sub-navbar — only renders once `streams.length > 0`). In their place, the header card now holds a "Dashboard"/"Detailed" view toggle, styled to match the "Add Income Stream" button exactly (same `px-4 py-2.5 rounded-lg text-[13px] font-semibold` sizing, same solid green fill on the active state — dark `bg-[#10b981]`/light `bg-[#059669]`, white text — per explicit user request after an initial pastel-pill version). "Detailed" swaps the stat-cards/charts for a sortable-by-nothing (yet) table of individual income entries (mock data, columns Date/Source/Account/Tax Status/Amount). (2026-09-04)
- Added a filters row (own card, below the buttons row): a Date range segmented pill (1M/3M/12M/Custom — Custom reveals two native `<input type="date">` fields), an Account filter segmented pill (`BanknoteArrowDown` icon, user-picked over `Landmark`/`CreditCard`/`Wallet2`), and a Taxed/Non-Taxed/All segmented pill (text-only, no icon, matching the existing Taxable/Non-Taxable toggle's icon-less style). Date icon: `CalendarDays` (user-picked over `Calendar`/`CalendarRange`). Date range is still a UI-only placeholder; tax and account filters are wired and actually filter the Detailed table's rows. (2026-09-04)
- Account filter reworked from a dropdown (All Accounts/Checking · Chase/Savings · Ally/Cash) to a 3-way segmented pill toggle — All/Account/Cash — per explicit user request, matching the Taxed/Non-Taxed toggle's style. `entry.account === 'Cash'` distinguishes Cash rows from all other (bank) account rows in the mock detailed-entries data. (2026-09-04)
- "Add Income Stream" moved out of the buttons row below the header and into the header card itself, positioned directly right of the Dashboard/Detailed toggle (both right-aligned together) — "Ingest Payslip (PDF)" stays alone in the row below. Per explicit user request. (2026-09-04)
- Taxed/Non-Taxed filter given a `Landmark` icon (matching `CalendarDays`/`BanknoteArrowDown` on the other two filters) — reuses the icon already used for the Taxes nav item in the Sidebar, since the earlier "no icon" option was superseded by explicit user request. (2026-09-04)
- "Ingest Payslip (PDF)" button removed from its own row and relabeled "Add Payslip"; now lives at the right edge of the filters row (`ml-auto`), icon swapped from `FileText` to `FilePlusCorner`. Styled as a solid filled button (dark `bg-[#10b981]`/light `bg-[#059669]`, white text) rather than its previous bordered/outline look, matching the existing solid "Add Payslip" button convention already used in `IncomePage.tsx`'s per-stream sub-navbar. (2026-09-04)
- "Add Payslip" now renders in both Dashboard and Detailed view (the `viewMode === 'detailed'`-only restriction from the same day was reverted per follow-up user request). Clicking it opens a new modal (`IncomeOverview.tsx`, self-contained state) with: Income Stream (required `<select>`, populated from the `streams` array now passed down from `IncomePage.tsx` — disabled with a single "Add Income Stream" placeholder option plus a "+ Add an income stream first" link (closes this modal, opens the existing Add Income Stream modal) when `streams.length === 0`), Income Type (Account/Cash pill toggle, defaults Account — labelled "Type" until a follow-up request renamed it), Tax Status (Taxed/Non-Taxed pill toggle — auto-resets to Taxed when Income Type is set to Account, Non-Taxed when set to Cash, but stays user-overridable afterward; labelled "Status" until the same rename), Date (optional `<input type="date">`), and Upload Payslip / Proof of Income (optional `<input type="file" accept=".pdf,image/*">`, styled as a dashed drop-zone-look label, placeholder text just "Choose a file" — trimmed from "Choose a file (PDF or image)" per follow-up request). Submit disabled until an Income Stream is chosen. No backend yet — submission goes through a new `onSubmitPayslip` prop, default no-op console.log, same placeholder pattern as the rest of the app; the old `onIngestPayslip` prop was removed since nothing else used it. (2026-09-04)
- Date range filter gained three more pill options — Current FY, Previous FY, Next 12M — alongside the existing 1M/3M/12M/Custom, per explicit user request. UI-only so far (no actual date-math wired to any of the pills yet, same as the pre-existing 1M/3M/12M). (2026-09-04)
- Add Payslip modal's single `<input type="date">` replaced with three separate Day/Month/Year `<select>`s (3-col grid), so a payslip can be logged with just month + year (day left as "Day" placeholder) — per explicit user request, since a native date picker forces a full date. Year options span the current year back 6 years. On submit the three parts are joined into a `YYYY-MM-DD` string (day defaults to `01` if left blank). (2026-09-04)
- Month and Year on that Date field made required (Day stays optional) per follow-up request: label now shows a red `*` with a "(day optional)" hint, both selects carry the `required` attribute, and Submit stays disabled until Income Stream + Month + Year are all set. (2026-09-04)
- Added an Income Stream filter to the Detailed view's filters row, as a native `<select>` dropdown (not a segmented pill like the other filters — user explicitly asked for "a dropdown") with an "All Income Streams" default option. Icon: `LayersArrowDown` (lucide-react, user-picked directly by icon name rather than from the offered `Layers`/`Wallet2`/`ListFilter` options). Options are the unique `source` values from the mock `DETAILED_ENTRIES` data (Primary Employment, Dividends & Capital Yield, Consulting & Advisory). **Superseded same day, see below.** (2026-09-04)
- The above filters-row Income Stream dropdown was removed (user called it "incorrect") and replaced by folding stream selection directly into the header card's view toggle: the "Detailed" button was swapped for a dropdown in the same slot, active whenever `viewMode === 'detailed'`, with "All Income Streams" as its default option plus one option per unique `DETAILED_ENTRIES` source. Selecting any option (or clicking the control at all, via `onClick`) switches `viewMode` to `'detailed'` and sets the stream filter to that selection in one action — realizing the original ask ("make Detailed a dropdown to select an income stream") more directly than a separate filter control. "Dashboard" stays a plain button alongside it. The `streamFilter` state and its filter-predicate wiring into the Detailed table are unchanged, just relocated. (2026-09-04)
- That dropdown's active state was briefly changed from the solid `viewToggleActive` fill to the flat pastel `pillActive` style per user request ("dropdown should not become complete green"). Discovered in the process: styling a native `<select>` element's background/text color directly also colors its native option-list popup in most browsers — which was the real cause of the dropdown "sometimes becom[ing] incorrect" (the whole open menu rendering solid green, per user screenshot). Fixed with the standard overlay pattern: a plain styled `<div>` renders the visible pill (text + a `ChevronDown` icon), with the real `<select>` positioned `absolute inset-0 opacity-0` on top purely to capture clicks/selection — so the native popup renders with default browser styling instead of inheriting app colors. (2026-09-04)
- Follow-up: the popup text was still turning green, because the invisible `<select>` inherits `color` by CSS inheritance from its parent pill `<div>`, and browsers use the select's own computed color to render option-list text. Fixed by pinning the select's own text color directly (`style={{ color: isDark ? '#e5e2e1' : '#0b1c30' }}`), decoupling the native popup's text color from whatever active/inactive class is on the visible wrapper `<div>`. With that decoupling in place, the wrapper's active style was switched back to the solid `viewToggleActive` fill (matching the "Dashboard" button's active look exactly) per explicit follow-up request — safe now since the popup no longer inherits color from it. (2026-09-04)
- Clicking "Dashboard" now also resets `streamFilter` back to `'all'`, so returning to the Dashboard tab and then back to the stream dropdown doesn't leave a stale per-stream filter selected underneath the "All Income Streams" default label. (2026-09-04)
- `IncomePage.tsx` and `IncomeOverview.tsx` moved from `src/components/` to `src/pages/income/` (first use of a `pages/` directory in the project — everything else so far lives flat under `components/`), per explicit user request to group Income-page code together. Internal import between the two files is unchanged (still same-directory); their `BANK_OPTIONS` import from `src/constants/banks.ts` updated from `../constants/banks` to `../../constants/banks`; `App.tsx`'s `IncomePage` import updated to `./pages/income/IncomePage`. (2026-09-04)
- Added an Income Type (Account/Cash) pill toggle to the "Add Income Stream" modal (`IncomePage.tsx`) — it didn't have one before (only the Add Payslip modal did). `IncomeStream` gained `type: 'account' | 'cash'` and `bank: string` fields. In both the Add Income Stream modal and the Add Payslip modal, a "Bank" `<select>` now sits directly next to (same row, second column of a `flex gap-4`) the Income Type toggle, shown only when Income Type is "Account" (hidden entirely for Cash, and reset to the default when switching to Cash). Bank options live in a new shared `src/constants/banks.ts` (`BANK_OPTIONS`), used by both modals; list uses Indian banks (State Bank of India, HDFC Bank, ICICI Bank, Axis Bank, Punjab National Bank, Bank of Baroda, Kotak Mahindra Bank, Canara Bank, Union Bank of India, IDBI Bank, Yes Bank, IndusInd Bank, Other) per explicit user request, with "None" as the default/first option. `PayslipFormData` gained a matching `bank` field. (2026-09-04)
- Both the Add Income Stream modal and Add Payslip modal widened from `max-w-sm` to `max-w-xl` (384px → 576px, exactly +50%) per explicit user request. (2026-09-04)
- Add Income Stream modal's Tax Status toggle labels changed from "Taxable"/"Non-Taxable" to "Taxed"/"Non-Taxed", matching the wording already used by the Add Payslip modal's Tax Status toggle and the Detailed view's Taxed/Non-Taxed filter pill — one consistent tax-status label app-wide. Underlying state/prop names (`taxable`, `newStreamTaxable`) unchanged, label text only. (2026-09-04)
- Both remaining "Income Type" field labels (Add Income Stream modal in `IncomePage.tsx`, Add Payslip modal in `IncomeOverview.tsx`) renamed to "Source Type", matching the new backend's `SourceType` naming below. Label text only — underlying state/prop names (`newStreamType`, the Account/Cash toggle) unchanged. (2026-09-05)

## Settings Popup

- Added `SettingsModal.tsx`, opened from the Sidebar's existing "Settings" button (previously a `console.log` placeholder — `App.tsx` now owns a `settingsOpen` boolean). Five sections, each a flat pastel-pill toggle matching the app's existing modal style: Base Currency (Rupee/Euro/Dollar, `IndianRupee`/`Euro`/`DollarSign` icons, defaults Rupee), Default Theme (System/Light/Dark, `Monitor`/`Sun`/`Moon` icons — `Monitor`, `FolderOpen`, and `Download` were picked from offered icon options), Theme Color (Green/Violet/Orange swatches), Data Directory (folder path display + "Browse" button), Export Data (solid "Download" button). (2026-09-05)
- **Theme is now driven by a `themeMode: 'system' | 'light' | 'dark'` state in `App.tsx`** (default `'system'`, matching the existing "default theme = system preference" decision above), rather than the old plain `theme: 'dark' | 'light'` state. `theme` is derived (`themeMode === 'system' ? systemTheme : themeMode`), with `systemTheme` tracked live via a `matchMedia('(prefers-color-scheme: dark)')` change listener (previously only read once via `getSystemTheme()` at mount). The Header's own light/dark quick-toggle button now sets `themeMode` directly to `'light'`/`'dark'` (an explicit override away from system) — both controls share one source of truth. Fixed a latent bug this surfaced: `Header.tsx` held its own internal `currentTheme` state that only initialized from the `theme` prop once and never re-synced — added a `useEffect` to keep it in sync, otherwise theme changes made from the Settings popup wouldn't render in the Header. (2026-09-05)
- **Theme Color is fully wired app-wide** (user explicitly chose this over a UI-only placeholder). New `src/constants/accentColors.ts` defines a `green | violet | orange` palette (`getAccentTokens(theme, accent)`), each with dark/light `solid`/`solidHover`/`gradientFrom`/`gradientTo`/`pastelBg`/`pastelText`/`hoverText`/`tintBg`/`tintBorder` tokens — violet and orange are new Tailwind-scale-derived palettes (violet-500/600 solid, violet-950/100 pastel, etc.; same shape for orange) sized to match the existing green (emerald) palette's roles. An `accent: AccentColor` prop (default `'green'`) now threads through `Sidebar`, `Header`, `ChatButton`, `ChatPopup`, `IncomePage`, and `IncomeOverview`.
  - **Mechanism — CSS custom properties, not interpolated Tailwind classes.** Tailwind's compiler statically scans source for literal class strings; a template-literal class like `` `bg-[${accentHex}]` `` never generates CSS since the value isn't known at build time. Instead, each accent-aware component sets `--accent-solid`, `--accent-solid-hover`, `--accent-pastel-bg`, `--accent-pastel-text`, etc. as inline `style` custom properties once (on the component's root, or wherever needed for smaller elements like the Sidebar logo badge), then uses **static** Tailwind arbitrary-value classes that reference them, e.g. `bg-[var(--accent-solid)] hover:bg-[var(--accent-solid-hover)]` — the literal string is what Tailwind scans, the CSS variable is what resolves the actual color at runtime. Plain inline `style={{ color: t.solid }}` is used instead for non-interactive elements (no hover state needed, e.g. the "Matter" logo text, avatar circle, active nav pill). Any future accent-driven color should follow this same pattern, not go back to hex-in-className interpolation.
  - `IncomeOverview.tsx`'s separate `ACCENTS` (green/blue/amber/pink) palette for stat-card/chart category coloring (Total Monthly Inflow, Primary Employment, Dividends, Consulting, the Taxed/Non-Taxed table badge) is **intentionally left untouched** — it's a fixed data-visualization palette unrelated to the app's single brand accent, not something the Theme Color setting should touch.
  - Verified visually (Playwright, screenshots) that switching Theme Color updates the Sidebar active pill/logo, Header avatar/⌘K badge, ChatButton/ChatPopup, and both Income-page files consistently across dark/light. Caught and fixed one wiring bug in the process: `IncomePage.tsx` wasn't forwarding its new `accent` prop down to `<IncomeOverview>`, so the Income Overview page (header card, filter pills, Diversity Index badge, Add Income Stream/Payslip buttons) stayed green regardless of the selected Theme Color until fixed. (2026-09-05)
- Base Currency, Theme Color, and Data Directory are plain in-memory React state in `App.tsx` (no `localStorage`/backend persistence yet), matching this app's existing pattern of shipping UI-first with unwired/lightly-wired backing state (same as the rest of the app's placeholder handlers). They don't retroactively reformat the hardcoded mock `$` amounts elsewhere in the app (e.g. Income Overview's stat cards) — that requires a real currency-formatting/data layer, out of scope here.
- Data Directory uses the browser's File System Access API (`window.showDirectoryPicker()`, Chromium-only) when available — the picker only exposes the selected folder's `name`, not a full filesystem path (a deliberate browser sandboxing restriction). On unsupported browsers (Firefox/Safari) it falls back to a hidden `<input type="file" webkitdirectory>`, so "Browse" still opens the OS's native file explorer for manual folder selection everywhere (rather than a `window.prompt()` text entry) — folder name is read from the first selected file's `webkitRelativePath`. Same `name`-only limitation applies (no full path) since that's a browser-wide sandboxing restriction, not specific to either API. (2026-09-05)
- Export Data's "Download" button generates a client-side JSON file (`moneymatter-export.json` via `Blob` + `URL.createObjectURL`) containing the current profile and settings (base currency, theme mode, accent) — there's no real transaction/financial data model yet to export, so this is a working placeholder scoped to what state actually exists today. Revisit once real financial data exists.

## Backend / Data Storage

- Local data root directory named `Database` (not `Money_Matter_Data`, per explicit rename request), resolved via `os.UserConfigDir()` (e.g. `%AppData%/Database` on Windows) and created on first run. Contains one master `profiles.db` (SQLite, via `modernc.org/sqlite` — promoted from indirect to a direct go.mod dependency) plus a `profiles/<profileId>/` folder per profile, each with `db/` (that profile's own income/expense/investment/taxes SQLite databases, not yet built) and `income/`, `expense/`, `investment/`, `taxes/` folders for that area's uploaded source documents. (2026-09-05)
- Renamed the data root directory again, from `Database` to `MoneyMatter_Database` (`internal/store/paths.go`'s `DataDirName`), per explicit rename request. (2026-09-05)
- `profiles` table column order is `id, name, color, currency, base_theme, theme_color, is_admin, notifications` — `name` and `color` placed right after `id`, per explicit request (initial implementation had `color` before `name`). `id` is the primary key (not null) and is the foreign key every future per-profile database uses to scope its rows back to a profile. `notifications` (nullable) is stored as a JSON-encoded string array — SQLite has no native array column type. Every fresh install is seeded with a fixed `guest` profile (id `"guest"`) so the app always has at least one usable profile. Implemented in `internal/store` (`paths.go`, `profile.go`); wired into `App` in `app.go` (`ListProfiles`/`CreateProfile`/`UpdateProfile`/`RenameProfile` bound methods) — not yet consumed by the frontend, which still has its own local unwired `Profile` type/mock data. (2026-09-05)
- Profile ids are always server-generated (`uuid.NewString()`, via `github.com/google/uuid`, now a direct dependency) — `Create` ignores any `id` sent from the UI, so the frontend never invents its own profile ids. (2026-09-05)
- Added `is_admin` (nullable INTEGER, default 0/false) to `profiles`, exactly one profile can be admin at any time: `Create`/`Update` clear every other profile's `is_admin` first, inside the same transaction, whenever the incoming profile has `IsAdmin = true`. The first-ever profile (the seeded `guest` profile) starts as admin; that flag then moves to whichever profile is next explicitly marked admin. (2026-09-05)
- `name` is now `UNIQUE NOT NULL` and **is the profile's on-disk directory name** (`<dataDir>/profiles/<name>`, sanitized for filesystem-illegal characters) instead of the profile id — per explicit request that profile folders read as their name rather than a UUID. Renaming a profile is deliberately its own operation, `ProfileStore.RenameProfile(id, newName)` / `App.RenameProfile`, which updates the DB row and `os.Rename`s the directory together (best-effort rollback of the DB name if the directory rename fails); the general `Update` method intentionally excludes `name` from its `SET` clause so no other code path can change the name without also moving the directory. Duplicate names surface as `store.ErrProfileNameTaken`. Covered by `internal/store/profile_test.go`. (2026-09-05)
- **Frontend wired to the real profile backend** — `App.tsx` no longer holds mock profile data; it calls the generated `frontend/wailsjs/go/main/App` bindings directly (`ListProfiles` on startup, `CreateProfile`/`RenameProfile`+`UpdateProfile` from the existing Header/ProfileModal add/edit flow). Bindings were regenerated via `wails build -s -nopackage` (Go-only build, skips the frontend/packaging steps) after `app.go` gained its bound methods — they previously only exposed the template's `Greet`. `App.tsx` keeps the full `store.Profile` list in state (`rawProfiles`) since `UpdateProfile` overwrites every settable column, not just the changed one; the existing display-only `{id, name, avatar, color}` `Profile` type (now imported from `ProfileModal.tsx` instead of redeclared) is derived from it on render. On startup the profile with `isAdmin` true (the seeded guest) becomes `currentProfile`. (2026-09-05)
- **Settings (Base Currency/Default Theme/Theme Color) are now persisted per-profile**, not just in-memory `App.tsx` state — using the `currency`/`base_theme`/`theme_color` columns that already existed on `profiles` for exactly this. Switching the active profile adopts its stored values (falling back to the existing in-memory defaults if a column is empty, e.g. for profiles created before this wiring); changing a setting in `SettingsModal` (or the Header's quick dark/light toggle) updates local state immediately and fires `UpdateProfile` in the background to persist it. `handleExportData`'s payload is unaffected (still exports current in-memory profile+settings). (2026-09-05)
- `ProfileModal`'s Add/Save button now round-trips through the backend (`CreateProfile`/`RenameProfile`/`UpdateProfile`) instead of updating local state synchronously — added `saving`/`error` props so the modal can show "Saving…" and surface a failure (e.g. `store.ErrProfileNameTaken` on a duplicate name) inline instead of closing. `Header.tsx` owns the `profileSaving`/`profileError` state around the async save call, since it already owns the modal's open/close state. (2026-09-05)

- **Export Data now exports the real profile directory**, not the earlier client-side JSON placeholder. New `internal/store/export.go`: `ProfileStore.ExportProfile(id)` zips `<dataDir>/profiles/<name>` (its `db/` SQLite files plus the `income/`/`expense/`/`investment`/`taxes` upload folders) in memory via `archive/zip`, returning a `store.ProfileExport{Filename, Data}` (`<sanitized-name>-export.zip`). Bound as `App.ExportProfileData(profileID)`; `App.tsx`'s `handleExportData` calls it for `currentProfileRaw.id`, wraps the returned byte array in a `Blob` (`application/zip`), and triggers the same `<a download>` click pattern the old JSON export used — so the download mechanism is unchanged, only the payload (real zipped files instead of a JSON snapshot of in-memory settings) and MIME type. Covered by `internal/store/export_test.go`. (2026-09-05)
- `paths.DataDir()` (`internal/paths/paths.go`) switched from `os.UserConfigDir()` (`%AppData%/MoneyMatter_Database` on Windows, etc.) to resolving `MoneyMatter_Database` next to the running executable (`os.Executable()` + `filepath.EvalSymlinks`, then `filepath.Dir`), per explicit user request to keep the app portable — data travels with the binary instead of living in a hidden per-user OS config folder. No migration of existing installs' data was performed (old `%AppData%` data is simply orphaned, not moved). (2026-09-06)

## Support Popup

- Added `SupportModal.tsx`, opened from the Sidebar's existing "Support" button (previously a `console.log` placeholder — `App.tsx` now owns a `supportOpen` boolean), same modal shell/positioning as `SettingsModal.tsx`. Two sections: Basic Details (`Info` icon — app name + hardcoded placeholder version `v1.0.0` since `package.json`'s version is still `0.0.0`; a "Contact Support" row with a `Mail` icon and a `mailto:support@moneymatter.app` placeholder address) and Privacy Policy (`ShieldCheck` icon, static placeholder policy text reiterating the app's existing "100% Private & Offline" positioning — data stored locally only, no upload/sync/tracking). Icons user-picked from offered options over `BadgeInfo`/`CircleHelp` (details), `Headset`/`MessageCircleQuestion` (contact), `FileLock`/`ScrollText` (privacy). No backend/real policy document yet — same "ship UI-first with placeholder content" pattern as the rest of the app. (2026-09-05)

- **`internal/store` split into two packages**, per explicit request for a cleaner Go folder structure now that it held three unrelated concerns in one flat package: `internal/paths` (package `paths`) keeps only the generic, profile-agnostic OS data-dir resolution (`DataDir`, `DataDirName`); `internal/profile` (package `profile`) keeps everything profile-specific (`store.go`: the `Profile` struct + SQLite-backed `Store`/CRUD/migrations; `dirs.go`: `sanitizeDirName`/`profileDir`/`EnsureProfileDirs`; `export.go`: the zip export). `ProfileStore` renamed to `Store` (avoids the `profile.ProfileStore` stutter now that the package itself is named `profile`); `GuestProfileID`, `ErrProfileNotFound`, `ErrProfileNameTaken`, `RenameProfile`, `ExportProfile` kept their names since they don't literally repeat the package name as a prefix. The export payload type was *not* renamed to the equally-non-stuttering `Export` — `wails generate module` refuses to emit a frontend type for anything named `Export` (treated as a reserved JS keyword) — so it stays `ProfileExport`. `app.go` and `frontend/src/App.tsx` (which imported the generated `store` namespace from `wailsjs/go/models` directly) updated accordingly; bindings regenerated via `wails generate module`. (2026-09-05)

- **Added `internal/income`** (package `income`), the first per-profile database: `income.db`, living at `<dataDir>/profiles/<name>/db/income.db`, opened and closed per call from `app.go` rather than held open for the app's lifetime (a long-lived handle on a profile's own db file would risk colliding with `RenameProfile`'s `os.Rename` of that same directory on Windows). Schema is exactly as specified: `income_streams` (`id` PK, `profile_id` FK, `name` UNIQUE NOT NULL, `source_type` enum {both, account, cash}, `tax_status` enum {both, taxed, non_taxed}, `bank_account` required when source type is account or both, `last_updated`) and `payslips` (`id` PK, `name` UNIQUE NOT NULL, `income_stream_id` FK → `income_streams.id`, `date` — `MM/YY` or `DD/MM/YY`, validated in Go — `source_type`/`tax_status` restricted to {account, cash}/{taxed, non_taxed} (no "both" on a single payslip), `bank_account` required when account, `directories` — a JSON-encoded string array of uploaded-file paths, `last_updated`). `Stream.Name` is immutable via `UpdateStream` (excluded from its `SET` clause) since it's what the stream's on-disk document folder is named after. (2026-09-05)
- Extracted `sanitizeDirName`'s regex/logic out of `internal/profile/dirs.go` into a new exported `paths.SanitizeDirName(name, fallback string)` in `internal/paths`, so both `profile` (directories per profile name) and the new `income` package (directories per income-stream name) sanitize identically without duplicating the regex. `profile.sanitizeDirName` is now a thin wrapper calling it with fallback `"profile"`. (2026-09-05)
- Added `profile.Store.DBDir(id)` and `.IncomeDocsDir(id)` so other packages can locate a profile's `db/` and `income/` folders without reaching into `profile`'s private `profileDir` helper. Per the "interdirectory for each income stream" request, `income.EnsureStreamDir(profileIncomeDir, streamName)` creates `<profile>/income/<sanitized stream name>/` — called from `App.CreateIncomeStream` right after the stream's DB row is inserted — as the destination for that stream's uploaded payslip files (the paths a `Payslip.Directories` entry would point at). (2026-09-05)
- `App` gained bound methods `ListIncomeStreams`, `CreateIncomeStream`, `UpdateIncomeStream` (all keyed by `profileId`/`Stream.ProfileID`) and `ListPayslips`, `CreatePayslip`, `UpdatePayslip` (these three take an explicit `profileID` argument rather than a struct field, since the `payslips` table itself has no `profile_id` column per the given schema — it's only used to pick which profile's `income.db` to open). Bindings regenerated via `wails generate module`.
- **`IncomePage.tsx` wired to the real income backend**, following the exact same `isMockMode()`/mock-fallback pattern already established for profiles (`App.tsx`/`src/mocks/profiles.ts`): `IncomePage` now takes a required `profileId` prop (passed from `App.tsx` as `currentProfileRaw.id`) and, on mount/profile change, calls `ListIncomeStreams`/`ListPayslips` (or loads `MOCK_INCOME_STREAMS`/`MOCK_PAYSLIPS` from new `src/mocks/income.ts` under `?mock=true`) instead of starting from empty local state. The `IncomeStream` local type was dropped in favor of the generated `income.Stream` directly (its `id`/`name` shape already satisfies `IncomeOverview.tsx`'s `IncomeStreamOption`, so no mapping needed there). Submitting the Add Income Stream modal now calls `CreateIncomeStream` (or synthesizes a `mock-stream-<timestamp>` id in mock mode) instead of just pushing to local state, and shows an inline error (e.g. on a duplicate name) instead of silently failing. The per-stream tab's placeholder "No payslips yet" text was replaced with an actual list of that stream's payslips (date / bank-or-Cash / Taxed-Non-Taxed pill) once any exist. (2026-09-05)
- **`IncomeOverview.tsx`'s Add Payslip modal now actually persists**, via a new `IncomePage.handleSubmitPayslip` that converts the modal's joined `YYYY-MM-DD` date to the backend's `DD/MM/YY` format, looks up the chosen stream, and calls `CreatePayslip(profileId, ...)` (or synthesizes a mock id) before appending the result to local state — replacing the old no-op `console.log` default. The Dashboard/Detailed charts and `DETAILED_ENTRIES` table in `IncomeOverview.tsx` are **intentionally left as mock data** — the payslip schema (per explicit spec) has no amount/currency column at all, so there's nothing real yet to back those `$` figures with; only the plain per-stream payslip list above is backed by real data.
- The picked file in "Upload Payslip / Proof of Income" is stored as just its filename (`[file.name]`) in `Payslip.Directories` — there's no bound method yet to send the actual file bytes across the Wails bridge and write them under `<profile>/income/<stream>/`, so this is a placeholder (matches the file's actual path only once real file-upload plumbing is built), not a real saved document path yet.
- Verified in a headless-Chromium pass against `npm run dev -- ?mock=true`: the "Source Type" label renders (not "Income Type") in both the Add Income Stream and Add Payslip modals, creating a stream adds and selects its tab, submitting a payslip closes the modal and the new entry immediately shows up in that stream's tab list alongside the seeded mock ones, with zero console errors throughout. Not tested against the real Wails-bound backend (that requires the desktop webview runtime) — covered instead by `internal/income/store_test.go`'s unit tests, which exercise the same Go code path. (2026-09-05)
- **`Payslip`'s single `Date` field split into two columns**, per explicit request: `MonthYear` ("MM/YY", required — the payslip's pay period) and `Day` ("DD", optional — the day-of-month within that period, e.g. when the exact payday is known). `Day` alone is never a complete date and is never filtered/sorted on; `MonthYear` is the only date-based filter, matching the existing Add Payslip modal's Month/Year-required-Day-optional fields (`IncomeOverview.tsx` already collected these separately — it just used to join them into one string before sending to the backend, now sends both fields as-is with no joining/reformatting). `Store.ListPayslips` gained an optional `monthYear` filter parameter alongside the existing `incomeStreamID` one (`App.ListPayslips` passes it through as a third argument); the Detailed view's Custom date-range filter (`customFromMonth`/`customFromYear`/`customToMonth`/`customToYear`, still UI-only/unwired) was already Month+Year-only, consistent with this. `internal/income/store.go`'s `migrate()` defensively adds the `month_year`/`day` columns via `ALTER TABLE ... ADD COLUMN` (ignoring "duplicate column") for any `income.db` created before this split, same pattern as `profile.Store`'s `is_admin` migration — the old `date` column is left in place, unused, on such databases. (2026-09-05)
- `IncomePage.tsx`'s top sub-navbar (per-stream pill tabs + its own "Add Payslip" button, plus the simple per-stream payslip-list view it switched to) was removed entirely, per explicit user request — `IncomeOverview` now renders unconditionally as the page's only content. The now-unreachable `activeTab`/`activeStream`/`activeStreamPayslips` state and the `FilePlus` import were removed as dead code; `payslips`/`setPayslips` (load + append-on-create) were left in place since `CreatePayslip`/`ListPayslips` still do real backend work even though nothing currently renders the array. (2026-09-05)
- `IncomeOverview.tsx`'s header-card stream dropdown (next to "Dashboard") now lists real income streams — `streams.map(stream => ...)` using `stream.name` as both value and label (matching `DETAILED_ENTRIES`'s mock `source` strings so the existing Detailed-table filter predicate keeps working unchanged) — instead of `DETAILED_SOURCES` derived from the mock table itself. `DETAILED_SOURCES` was removed as dead code. The "All Income Streams" default option was already present and is unchanged. (2026-09-05)
- The filters row's "Custom" date range switched from two native `<input type="date">` (From/To) to two Month+Year `<select>` pairs (reusing the existing `MONTH_OPTIONS`/`YEAR_OPTIONS` arrays already defined for the Add Payslip modal's date fields), per explicit user request to drop the day component from custom date filtering. State renamed `customFrom`/`customTo` → `customFromMonth`/`customFromYear`/`customToMonth`/`customToYear`; still UI-only, no date-math wired yet (same as the rest of the date-range pills). (2026-09-05)
- **Added Amount, Deductions and Tax fields to the Add Payslip modal**, all plain decimals, placed directly below the Date fields — Amount and Deductions always shown, Tax shown only when Tax Status is "Taxed" (hidden and cleared on submit for Non-Taxed payslips). Backend: `internal/income/store.go`'s `Payslip` struct gained `Amount`/`TaxAmount`/`Deductions float64`, new nullable `amount`/`tax_amount`/`deductions` REAL columns (with the same defensive `ALTER TABLE ... ADD COLUMN` migration pattern used for the earlier Day/MonthYear split), included in `CreatePayslip`/`UpdatePayslip`/`GetPayslip`/`ListPayslips`. None are required/validated at the store layer (existing test fixtures and other callers don't need to set them). Bindings regenerated via `wails generate module` — this also caught and fixed a stale `models.ts`: it still had the old single `date` field and a 2-arg `ListPayslips`, never regenerated after the Day/MonthYear split and the `monthYear` filter param were added to `app.go` earlier. (2026-09-05)
- **Add Payslip now auto-populates when opened while a specific income stream is already selected** (the header's stream dropdown next to "Dashboard", not on "All Income Streams"/Dashboard view) — Income Stream, Source Type, Bank, and Tax Status are pre-filled from that stream's own fields (`IncomeOverview.tsx`'s `handleOpenPayslipModal`, replacing the plain `setPayslipModalOpen(true)` on the button's `onClick`). Opening the modal with no stream selected still starts blank, unchanged. `IncomeStreamOption` gained optional `sourceType`/`taxStatus`/`bankAccount` fields to support this. (2026-09-05)
- **`Payslip.Amount` is now required to be greater than zero, enforced in `validatePayslip` (`internal/income/store.go`)** — supersedes the "none are required/validated at the store layer" note above for `Amount` specifically (`TaxAmount`/`Deductions` remain optional/unenforced). Reason: a float64 defaulting to `0` made "no amount entered" indistinguishable from "an explicit $0 payslip," which the store's own doc comment flagged as a known gap. `CreatePayslip`/`UpdatePayslip` now return an error for `Amount <= 0`; `store_test.go` fixtures that previously omitted `Amount` were given a valid placeholder value, and a new `TestPayslipRequiresPositiveAmount` covers the rejection. Mirrored client-side in `IncomeOverview.tsx`'s Add Payslip modal: the Amount `<input>` gained `required` and `min` raised from `"0"` to `"0.01"`, so an empty/zero amount is caught before submit instead of failing silently against the backend. (2026-09-05)
- **Amount's `min="0.01"` HTML attribute was never actually enforced** — the Add Payslip modal's Submit is a plain `<button onClick>`, not a `<form>` submit, so browser-native `required`/`min` constraint validation never ran; a `0`, negative, or empty Amount could reach `onSubmitPayslip` and only get rejected (silently, per the existing `catch (err) { console.error(...) }` in `IncomePage.handleSubmitPayslip`) once it hit the backend's `validatePayslip`. Added real JS-side validation in `IncomeOverview.tsx`: a `payslipAmountValid` check (`parseFloat(payslipAmount) >= 0.01`, empty/NaN/negative all invalid) now gates the Submit button's `disabled` state and shows an inline red "Amount must be at least 0.01." message once the field is non-empty and invalid, plus a red `*` on the label matching the Date field's required-marker style. (2026-09-05)
- **The Detailed-view table now renders real payslip data** instead of the static mock `DETAILED_ENTRIES` array (which has been removed) — `IncomeOverview` takes a new `payslips: PayslipEntry[]` prop (`IncomePage.tsx` passes its already-loaded `payslips` state through), filtered by the same stream/tax/account filters as before plus scoped to "the current income source" — i.e. whichever stream is selected in the header's stream dropdown ("All Income Streams" shows every stream's payslips, same as before). Rows are sorted newest-first by `MonthYear`+`Day`; Date is formatted from the split `MonthYear`/`Day` fields (e.g. "Jan 15, 2026"), Account shows the bank name or "Cash", and Amount uses the real `Payslip.Amount` field now that it exists — superseding the earlier note that the Detailed table "intentionally left as mock data" since there was "nothing real yet to back those $ figures with." An empty-state row ("No payslips recorded yet.") shows when there are no payslips at all. (2026-09-05)
- **Fixed a real-backend bug found while verifying the above**: on any profile database created before the Date→MonthYear/Day split, `CreatePayslip` always failed with `NOT NULL constraint failed: payslips.date` — the split's migration only ever added the new `month_year`/`day` columns (`ALTER TABLE ... ADD COLUMN`) and never touched the original `date TEXT NOT NULL` column, which the current INSERT statement doesn't set. This is why a payslip appeared to save (modal closed, no visible error — `IncomeOverview`'s `handleSubmitPayslip` closes the modal immediately without awaiting the async `onSubmitPayslip`) but never showed up in the Detailed table: `CreatePayslip` was throwing, caught and only `console.error`'d in `IncomePage.handleSubmitPayslip`, so `setPayslips` never ran. Fixed with `Store.dropLegacyDateColumn()` (`internal/income/store.go`), run at the end of `migrate()`: SQLite can't drop a NOT NULL constraint via `ALTER TABLE`, so it rebuilds the `payslips` table via the standard create-new/copy/drop-old/rename procedure whenever the legacy `date` column is still present (detected via `PRAGMA table_info`), backfilling `month_year` from the old `date` value only if `month_year` itself is empty. Verified by reproducing the exact failure against a real `wails dev` build hitting the actual on-disk `Guest` profile database (which predates this split), then confirming the fix; covered by a new `TestCreatePayslipSucceedsAgainstLegacyDateColumn`, which hand-creates the pre-split schema and asserts `CreatePayslip` now succeeds against it. (2026-09-05)
- **Added Deductions and Tax columns to the Detailed table**, sitting after Amount (matching the Add Payslip modal's own Amount → Deductions/Tax field order) and reading from `Payslip.Deductions`/`Payslip.TaxAmount` now that both exist. A zero/absent value renders as "—" rather than "$0.00"; Tax additionally shows "—" for a Non-Taxed row regardless of any stored `taxAmount` value, since tax isn't applicable there. `PayslipEntry` gained `taxAmount`/`deductions` fields to match. (2026-09-05)
- **Fixed the Account column showing the literal string "None"** for account-type payslips where the user left Bank at its default (`BANK_OPTIONS[0]` is the literal string `"None"`, not an empty string — see the 2026-09-04 Bank-options entry above). The column now falls back to "Account" whenever `bankAccount` is empty *or* exactly `"None"`, matching the intent of "show the bank if a real one was picked, otherwise just say it's an account." (2026-09-05)
- **Added per-row attachment actions (Add / View / Download) to the Detailed table**, icon-only buttons in a new rightmost "Attachment" column — icon set was chosen by the user from 3 offered options over a `FileUp`/`FileSearch`/`FileDown` "File\*" family and a more generic `CirclePlus`/`ExternalLink`/`ArrowDownToLine` set; Add's icon was then switched from the picked `Paperclip` to `FilePlusCorner` per explicit follow-up request, matching the existing "Add Payslip" button's icon (View/Download stayed `Eye`/`Download`). This also closed a real gap flagged repeatedly above: a payslip's `Directories` only ever held a picked file's bare *name* (`data.file.name`), never real bytes, since there was no bound method to write them to disk. Added two new bound methods in `app.go`: `SaveIncomeAttachment(profileID, streamName, fileName string, data []byte) (string, error)` (writes into `<profile>/income/<stream>/<fileName>` via the existing `income.EnsureStreamDir`/`income.StreamDir` helpers, returns the saved path) and `ReadIncomeAttachment(path string) ([]byte, error)` (reads it back) — bindings regenerated via `wails generate module` (`[]byte` maps to `Array<number>` in the generated TS, same as `ProfileExport.Data`). `IncomePage.tsx`'s new `attachFile` helper uploads a picked file (named `<payslipId>_<original filename>` to avoid collisions) and calls `UpdatePayslip` to append the returned path to `Directories`; it's shared by both the table's new per-row Add button and the Add Payslip modal's existing upload field, which previously only faked the filename (`CreatePayslip` now runs first, then the file is attached to the real created payslip using its server-generated id). View converts the read bytes to a `Blob` and opens it in a new tab (`URL.createObjectURL` + `window.open`); Download does the same but drives a synthetic `<a download>` click. View/Download are disabled (no attachment yet) until `directories` is non-empty; mock mode has no real backend to write to, so Add just records the picked filename locally (matching the existing mock-mode pattern) and View/Download log a "no real file backs this yet" message instead of erroring. (2026-09-05)
- **"Next 12M" removed from the Date range filter pills** (`DateRange` type and the pill list both dropped the `next12m` option), per explicit user request — the remaining pills are 1M/3M/12M/Current FY/Previous FY/Custom. (2026-09-05)
- **The Date range filter is now actually wired to the Detailed table** (previously noted above as "UI-only ... no actual date-math wired to any of the pills yet"). New `dateRangeBounds()` in `IncomeOverview.tsx` computes an inclusive `[from, to]` month range as comparable `year*12+month` keys: 1M/3M/12M are trailing windows ending at the current calendar month (12M spans the same 12 months as the Dashboard's "12-Month Inflow Trajectory" chart); Custom uses the four Month+Year `<select>`s verbatim (no filtering applied until all four are set). **Financial Year is April→March**, per explicit correction — Current FY spans April of this FY's start year through March of the next calendar year, where the FY start year is the current year if today's month is April or later, else last year (e.g. today Sep 2026 → Current FY = Apr 2026-Mar 2027, Previous FY = Apr 2025-Mar 2026). Table filtering was refactored into one `detailedRows` computation (used by both the row map and the empty-state check, previously duplicated inline), and the empty-state message now distinguishes "No payslips recorded yet." (nothing at all) from "No payslips match the current filters." (rows exist but none pass the active Date/Stream/Tax/Account filters) — previously it only ever checked the unfiltered `payslips.length`, so a filtered-to-zero table silently rendered no rows and no message. Verified against the mock payslips (Jan/Feb 2026, "today" 2026-09-05): 12M and Previous FY both show them, 3M and Current FY correctly show the new empty-filters message. (2026-09-05)
- **Added an "All" pill to the Date range filter, placed before 1M** (`dateRangeBounds` returns `null` for it, same as an incomplete Custom range, meaning no date filtering at all — every payslip passes regardless of period). Per explicit user request. (2026-09-05)
- **Added an edit button to each row of the header's stream dropdown**, opening the existing Add Income Stream modal prefilled for that stream, submitting via `UpdateIncomeStream` instead of `CreateIncomeStream`. Icon: `Pencil` (lucide-react, 14px, user-picked from `Pencil`/`SquarePen`/`PencilLine` options — same icon/size already used for the analogous edit button in the Header's "Switch Profile" dropdown). Row markup follows that same precedent: `role="button"` `<div>` (not `<button>`, so the nested edit `<button>` stays valid HTML) with `opacity-0 group-hover:opacity-100` reveal, edit click calls `stopPropagation()` so it doesn't also select that stream as the filter. `IncomeOverview`'s `IncomeStreamOption` interface is now exported and the component takes a new `onEditStream?: (stream: IncomeStreamOption) => void` prop (no-op default). `IncomePage.tsx`'s modal gained an `editingStreamId` state: title/submit-button label swap ("Edit Income Stream"/"Save Changes"), name field stays editable. (2026-09-05)
- **Renaming an income stream (changing its Name in the edit modal) is now actually supported**, superseding `UpdateStream`'s original "Name is intentionally excluded from the update" behavior (it moved the on-disk document directory, so a plain field update wasn't safe) — mirrors the existing `RenameProfile` pattern. New `Store.RenameStream(id, newName)` in `internal/income/store.go` updates just the `name`/`last_updated` columns. `App.UpdateIncomeStream` (`app.go`) now detects a changed name first, calls `RenameStream`, then moves the stream's document directory (`income.StreamDir` old→new via `os.Rename`, best-effort rollback of the db name on failure, or `EnsureStreamDir` if no directory existed yet) before applying the rest of the fields via the existing `Store.UpdateStream` — one bound method, no new Wails binding needed. (2026-09-05)
- **The Detailed view's Custom date range now filters with Month left blank**, per explicit user request — previously `dateRangeBounds` required all four of From-Month/From-Year/To-Month/To-Year before applying any filter at all. Month is now optional per side: a Year alone defaults From to January and To to December of that year; a side with no Year at all is left fully unbounded (`-Infinity`/`Infinity`) rather than nulling out the whole filter; only when *neither* side has a Year yet does the filter fall back to "no filtering" (same as before). (2026-09-05)
- **Added a per-row Delete button to the Detailed table's payslips**, red (`text-red-500`, `Trash2` lucide-react icon, user-picked over `Trash`/`X`), in a new rightmost "Actions" column (kept separate from the existing Attachment column's Add/View/Download icon group). Clicking opens a small confirmation modal ("Delete Payslip" + payslip's source name, Cancel/Delete) before anything is removed — `IncomeOverview` tracks the pending target in local `deleteTarget` state, confirming calls a new `onDeletePayslip?: (payslipId: string) => void` prop. Backend: new `Store.DeletePayslip(id)` (`internal/income/store.go`) plus a bound `App.DeletePayslip(profileID, payslipID)` (`app.go`) that looks the payslip up first, deletes its row, then best-effort-removes every file in its `Directories` (a missing file doesn't fail the call — the db row is the source of truth). Bindings regenerated via `wails generate module`. `IncomePage.tsx`'s `handleDeletePayslip` calls it (skipped in mock mode, same pattern as elsewhere) and filters the deleted id out of local `payslips` state. (2026-09-05)
- **`income.db` moved from `<profile>/db/income.db` to `<profile>/income/income.db`**, so a profile's income database now lives next to the per-stream document folders it describes rather than in the shared `db/` folder (which still holds/will hold the other per-profile databases — expense/investment/taxes — once built). `App.openIncomeStore` (`app.go`) now resolves the path via `profile.Store.IncomeDocsDir` instead of `.DBDir`, creating the `income/` folder defensively (`os.MkdirAll`) for profiles that predate this move. A new `App.migrateLegacyIncomeDB` runs first and `os.Rename`s an existing `db/income.db` into the new location the first time a profile's income store is opened after upgrading (no-op once migrated, and a no-op for profiles that never had one) — same "detect and migrate on open" precedent as `Store.dropLegacyDateColumn`, needed here since real dev data (the seeded Guest profile's `income.db`) already existed at the old path. (2026-09-05)
- **Payslip attachments are now saved as `<StreamName>_<Date>_<count>.<ext>`** instead of `<payslipId>_<original filename>`, per explicit user request — `Date` is the payslip's `MonthYear` (prefixed with `Day` when set, e.g. `15-01-26` or `01-26`), `count` is a running total of every attachment already saved across all of that stream's payslips (so each attachment gets a distinct number even across different payslips), and the extension is taken from the picked file. A new `sanitizeFileNamePart` helper in `IncomePage.tsx` strips filesystem-illegal characters and collapses whitespace to underscores for both the stream-name and date components before they're joined into the filename. (2026-09-05)
- **`IncomeOverview.tsx`'s Dashboard rebuilt to run on real payslip data instead of static mock stat cards/charts.** Row 1: the old 4 mock stat cards (Total Monthly Inflow/Primary Employment/Dividends/Consulting) were replaced with 3 real ones — Total Income, Total Deduction, Total Tax (icons `PiggyBank`/`MinusCircle`/`Landmark`, the latter two picked/confirmed by the user over alternatives) — each summed from `payslips` and badged with a real percent-change vs the previous period (not mock text); the "Income Diversity Index" donut moved up from the old chart row into this row's 4th slot, now sized by each real income stream's total `amount` (previously a 3-slice mock). Row 2 replaces the old fixed "12-Month Inflow Trajectory" composed chart with one toggleable bar chart — "Income Distribution" (Income/Deduction/Tax, **grouped** bars, per explicit user choice) vs "Income Diversity" (one **stacked** bar per income stream, per the same choice) — bucketed by month, or by year once the filtered data spans more than 23 distinct months. All of Row 1/Row 2 now read from the same `filteredPayslips` (Date range/Account/Tax/Stream filters already used by the Detailed table), so the Dashboard is filter-reactive rather than static. (2026-09-05)
- **New six-hue categorical palette for per-income-stream identity** (`STREAM_COLORS` in `IncomeOverview.tsx`: green/indigo/amber/cyan/violet/pink), used only for the donut and the new stacked "Income Diversity" bar chart — validated via the dataviz skill's `validate_palette.js` (fixed order, passes all checks — lightness band, chroma floor, CVD adjacent-pair separation, normal-vision floor, contrast — on both the light and dark chart surfaces with the same hex values, unlike `ACCENTS` which needs separate light/dark steps). Deliberately kept separate from the existing `ACCENTS` (green/blue/amber/pink) palette rather than extending it, since `ACCENTS`'s own green/pink pairing fails CVD separation and is explicitly "intentionally left untouched" per the entry above. Streams beyond the 6 color slots fold into one muted "Other" entry (summed) rather than reusing a color, so two real streams never share an identity color; slot assignment follows each stream's stable creation order, never sorted by value, so filtering/amount changes never repaint a stream's color. A normalized-Shannon-entropy "Income Diversity Index" score (0–1, labeled Low/Moderate/High) replaces the old hardcoded "High (0.84)" badge on the donut card.
- **The Income page now formats every money value using the active profile's actual currency** instead of a hardcoded `$`, closing the gap called out in the 2026-09-05 Settings Popup entry above ("They don't retroactively reformat the hardcoded mock `$` amounts elsewhere in the app"). New `src/constants/currency.ts` (`BaseCurrency`, `CURRENCY_SYMBOLS`, `currencySymbol()`) is the single source of truth for the symbol lookup; `SettingsModal.tsx` now re-exports `BaseCurrency` from there instead of declaring its own copy. `IncomeOverview`/`IncomePage` take a `currency?: BaseCurrency` prop (default `'INR'`) threaded from `App.tsx`'s existing `baseCurrency` state (already the profile's persisted `currency` column); `fmtMoney` and every other spot that hardcoded `$` (detailed table's Amount/Deductions/Tax, the bar chart's Y-axis ticks and tooltips) now use the resolved symbol.
- **Fallback/default currency is the Indian Rupee (₹), not the Dollar** — this app's primary/only client so far is Indian (matches `BANK_OPTIONS`'s Indian-bank-only list and the Header's mock notification amount already being in ₹). `currencySymbol()`'s fallback for an unset/unrecognized currency is `CURRENCY_SYMBOLS.INR`; `IncomeOverview`/`IncomePage`'s `currency` prop defaults to `'INR'`; both `MOCK_PROFILES` entries in `src/mocks/profiles.ts` use `currency: 'INR'` (the second mock profile was `'USD'`, changed to match). Multi-currency support itself (the `BaseCurrency` type, the Settings modal's Rupee/Euro/Dollar picker, `BASE_CURRENCIES` in `App.tsx`) is intentionally kept, not removed — only the *default/fallback* changed, per explicit user request. (2026-09-05)
- **Dashboard stat-card follow-ups (all per explicit user request), `IncomeOverview.tsx`:** the "Amount" table column and Add Payslip form field were renamed to "Income" then to "Received" (kept "Total Income"/"Income Diversity"/"Income Distribution" headings unchanged after later correction — the user drew a line between the payslip's earned-amount field, which became "Received", and the page's pre-existing "Income" headings/concepts, which stayed put). The stat cards' period-over-period comparison now adapts to the selected date filter — 1M/3M/12M/Current FY/Previous FY compare against the immediately preceding window of the exact same length ("+X% than last N months"), computed by re-filtering the full unfiltered `payslips` array by a shifted `[from, to]` window rather than the old fixed monthly/yearly bucket comparison (which is now only the fallback for "All"/an incomplete Custom range, where there's no one span to mirror). Each card also shows a "share of total income" line: Total Deduction/Tax as % of Total Income, Total Income itself as % of (Income+Deduction+Tax combined) — deliberately two different denominators, since "Income's share of Income" is tautologically 100% and not worth showing. The pct badge is colored by sign (green positive, red negative — new `red` entry added to `ACCENTS`) rather than by the card's own accent, with amber reserved for the neutral cases (exactly 0%, or "No prior amount to compare" when there's nothing to diff against). (2026-09-06)
- **Bar chart colors re-validated via the dataviz skill, `IncomeOverview.tsx`'s `ACCENTS`:** the Income Distribution chart's dark-mode bar colors (`green.bar.dark`/`amber.bar.dark`/`blue.bar.dark`) failed `validate_palette.js`'s dark lightness band (measured L 0.68–0.84 against the ~0.48–0.67 dark band — too pale against the dark surface). Fixed by using one hex per hue across both light and dark (`#059669`/`#d97706`/`#6366f1`, the last matching the app's existing Violet accent base) — passes lightness band + contrast in both modes; the green/amber adjacent pair's CVD separation (ΔE 7.9) sits in the "legal only with secondary encoding" floor band, satisfied by the chart's existing legend + tooltip labels and 2px surface-color gap between bars. (2026-09-06)
- **Income Diversity's chart type changed from a donut to a horizontal bar chart** (one bar per stream, `%`-of-total label at each bar's end, `diversityAxisWidth` sized to the longest stream name shown rather than a fixed width, so short names don't waste card space) — the Row 2 toggle's own "Income Diversity" mode (grouped bars, one series per stream) was also switched from stacked-per-month segments to the same grouped/rounded-bar style as "Income Distribution", for one consistent bar look across both charts. The old "Asset Resilience" eyebrow + duplicate "Income Diversity" heading were collapsed into a single heading with a `ChartBarBig` icon badge (pink accent, the one slot `ACCENTS` had free). (2026-09-06)
- **Per-entry Edit added to the Detailed table** — a `Pencil` icon at the end of the Attachment column's icon row (pushed to the cell's far right edge via `justify-between`, separated from the Add/View/Download/Remove attachment icon cluster), with a matching "Action" header label split from "Attachment" in the same `<th>`. Clicking it reopens the Add Payslip modal (retitled "Edit Payslip") fully prefilled from that row; submitting calls `UpdatePayslip` (via a new `IncomePage.handleSubmitPayslip` branch keyed on a `data.id` field added to `PayslipFormData`) and replaces the row in place instead of creating a new one. The row's own Delete button was removed (no more separate "Actions" column) — Delete now lives inside the modal itself (bottom-left, red, matching the Add/Edit Income Stream modal's convention), closing the edit modal and reopening the existing delete-confirmation dialog. The "Upload Payslip / Proof of Income" field disables itself (same treatment as the table's own "already added" state) once the entry being edited already has an attachment. (2026-09-06)
- **`Payslip` renamed to `Entry` end-to-end, per explicit user request after flagging the collision risk up front** (that `Income` was already taken by two other concepts on this page — the Income Stream category, and Total Income the aggregate value — so a bare rename would've made "Add Payslip" read as "Add Income" and, worse, produced a stuttering `income.Income` type inside the already-existing `income` Go package). User chose the full-scope option (UI text + frontend TS + Go backend + database), so this touches every layer:
  - **Go (`internal/income/store.go`):** `Payslip` struct → `Entry` (`income.Entry`, no stutter — same pattern as `Stream`); `CreatePayslip`/`UpdatePayslip`/`GetPayslip`/`DeletePayslip`/`ListPayslips` → `CreateEntry`/`UpdateEntry`/`GetEntry`/`DeleteEntry`/`ListEntries`; `ErrPayslipNameTaken`/`ErrPayslipNotFound` → `ErrEntryNameTaken`/`ErrEntryNotFound`; `validatePayslip`/`scanPayslip` → `validateEntry`/`scanEntry`; `validForPayslip()` → `validForEntry()`. The `payslips` table is now `income_entries` (mirroring the existing `income_streams` naming), with a new `renameLegacyPayslipsTable()` migration step (runs before the `CREATE TABLE IF NOT EXISTS`, detected via `sqlite_master`) so a profile's existing on-disk `income.db` gets its table renamed in place rather than starting a second, empty `income_entries` table — same detect-and-migrate precedent as the earlier legacy `date`-column drop (which itself was updated to rebuild `income_entries`, not `payslips`). Covered by a new `TestCreateEntrySucceedsAgainstLegacyPayslipsTable`, alongside the renamed existing test suite (`TestCreateEntry*`, `TestEntryNameUnique`, `TestUpdateEntry`, etc.) — full package suite passes.
  - **`app.go` bound methods:** `ListPayslips`/`CreatePayslip`/`UpdatePayslip`/`DeletePayslip` → `ListIncomeEntries`/`CreateIncomeEntry`/`UpdateIncomeEntry`/`DeleteIncomeEntry` (kept the fully-qualified `Income`-prefixed names here, unlike the internal package's bare `Entry` — App-level bound methods aren't namespace-qualified from the frontend's perspective, so bare "CreateEntry" would be ambiguous once Expense/Investment entries exist too). `DeleteIncomeAttachment` keeps its name (no stutter risk) but its payslip-id parameter/return type updated accordingly. Bindings regenerated via `wails generate module`.
  - **Frontend:** `PayslipEntry`/`PayslipFormData` (`IncomeOverview.tsx`) → `IncomeEntry`/`IncomeEntryFormData`; every `payslip*`-prefixed prop, state, and handler (`payslips`/`onSubmitPayslip`/`onDeletePayslip`/`handleSubmitPayslip`/`handleOpenEditPayslip`/etc.) → the `entry`/`Entry` equivalent; `IncomePage.tsx` and `mocks/income.ts` updated to match (`MOCK_PAYSLIPS` → `MOCK_INCOME_ENTRIES`). UI text updated too: "Add/Edit/Delete Payslip" → "Add/Edit/Delete Income Entry", "Income Stream & Payslip" page heading → "Income Stream & Entry". Two things were deliberately **not** renamed: "Upload Payslip / Proof of Income" (the file-upload label) and the Header's mock notification text "Payslip ingested" — both use "payslip" as the everyday English word for a real proof-of-income document, not as the app's internal entity name, so renaming them would read as if the document itself had changed rather than just its data-model label. (2026-09-06)
